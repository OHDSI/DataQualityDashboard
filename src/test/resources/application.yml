rserve:
  path:
  host: localhost
  port: 6311
  unix: true

files-manager:
  url:

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: false
